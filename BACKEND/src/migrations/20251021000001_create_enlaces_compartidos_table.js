/**
 * Migración: Create enlaces compartidos table (stub)
 * Nota: Esta migración fue registrada en la BD pero el archivo fue eliminado
 */

exports.up = async function (knex) {
  // Stub - ya existe la tabla en BD
  return Promise.resolve();
};

exports.down = async function (knex) {
  // Stub
  return Promise.resolve();
};
